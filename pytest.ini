[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: requires a live PostgreSQL (and is skipped when unavailable)
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
